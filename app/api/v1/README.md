# Reserved API v1 boundary

No API route is implemented in Sprint 0. Future mobile-ready HTTP contracts belong under `/api/v1`; route handlers must validate transport input and call feature application services rather than persistence or provider SDKs.
